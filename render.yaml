services:
  - type: worker
    name: reminder-bot
    runtime: python
    envVars:
      - key: RENDER
        value: "true"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
